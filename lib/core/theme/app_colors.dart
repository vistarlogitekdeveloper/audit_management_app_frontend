import 'package:flutter/material.dart';

class AppColors {
  // ── Vistar brand palette (derived from logo) ───────────────────
  static const Color primary = Color(0xFF8B1FA9);   // Vistar deep purple
  static const Color primaryDark = Color(0xFF6A1680); // darker shade for gradients
  static const Color primaryLight = Color(0xFFB94FD4); // lighter for hover states
  static const Color accent = Color(0xFFE8820C);    // Vistar orange from S-curve
  // ── Semantic palette ────────────────────────────────────────────
  static const Color secondary = Color(0xFF1D9E75);
  static const Color danger = Color(0xFFE24B4A);
  static const Color warning = Color(0xFFBA7517);
  static const Color success = Color(0xFF3B6D11);
  static const Color purple = Color(0xFF8B1FA9);    // alias = primary
  static const Color info = Color(0xFFF5E8FA);      // purple-tinted info surface

  static const Color background = Color(0xFFF8F9FA);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color border = Color(0x1F000000);

  static const Color textPrimary = Color(0xFF18212F);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color inactive = Color(0xFFF3F4F6);
  static const Color overlay = Color(0x80000000);

  static const Color passBackground = Color(0xFFEAF3DE);
  static const Color failBackground = Color(0xFFFCEBEB);
  static const Color naBackground = Color(0xFFF1EFE8);
  static const Color blueTint = Color(0xFFF5E8FA);  // purple tint (replaces blue)
  static const Color amberTint = Color(0xFFF8EEDC);
  static const Color redTint = Color(0xFFFBE8E8);
  static const Color greenTint = Color(0xFFEAF3DE);
  static const Color greyTint = Color(0xFFF4F5F7);

  static const Color passBorder = Color(0xFF1D9E75);
  static const Color failBorder = Color(0xFFE24B4A);
  static const Color naBorder = Color(0xFFB0A89A);

  static const Color white = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE5E7EB);
  static const Color black = Color(0xFF000000);
}
